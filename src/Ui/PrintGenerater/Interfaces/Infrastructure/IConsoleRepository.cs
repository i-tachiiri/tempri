
/*namespace TempriDomain.Interfaces;

public interface IConsoleRepository
{
    string? WriteAndRead(IEnumerable<string> lines, bool isReadLine);

    void ExceptionLog(string message);
    void ActivateConsole();
    public void TaskLog(Action<IPrintMasterEntity> task, IPrintMasterEntity entity);
    public void TaskLog(Action<IPrintMasterEntity, bool> task, IPrintMasterEntity entity, bool IsTestCase = false);
    public Task TaskLog(Func<IPrintMasterEntity, Task> task, IPrintMasterEntity entity);
    public Task TaskLog(Func<IPrintMasterEntity, bool, Task> task, IPrintMasterEntity entity, bool IsTestCase = false);
    public void WriteLog(string message)
}*/

